# Finnhub Economic Calendar: certificación pendiente

El híbrido oficial posterior también permanece **NOT_CERTIFIED**: BLS ICS responde Access Denied; Fed y BCE RSS documentan comunicados publicados pero no la agenda futura de decisiones, y Eurostat ICS observado aporta fecha sin hora. BEA ICS sí aporta eventos futuros PIB/PCE con hora. Véase `AUDIT_POST_PHASE7.md` para evidencia por USD/EUR. `AI_FLOOR_MACRO_PROVIDER=none` sigue configurado; no se reintentaron Finnhub ni FRED.

Proveedor implementado: `FinnhubMacroDataProvider` con endpoint oficial `GET /api/v1/calendar/economic?from=...&to=...` y header `X-Finnhub-Token`. Referencia: [API oficial](https://finnhub.io/docs/api/economic-calendar) y [SDK oficial](https://github.com/Finnhub-Stock-API/finnhub-python/blob/master/finnhub/client.py).

Contrato normalizado versión 1.0: `event_id`, `source`, `event_time_utc`, `country`, `currency`, `event`, `importance`, `actual`, `previous`, `consensus`, `fetched_at` y `source_timestamp` si existe. Se rechazan timestamps sin zona horaria; se filtran USD/EUR; eventos futuros permiten awareness pero `actual` no se entrega antes del release ni antes de la captura. Caché de una hora para evitar una consulta por ciclo. Errores `NOT_CONFIGURED`, `AUTH_ERROR`, `RATE_LIMITED`, `PROVIDER_FAILURE`, `INVALID_RESPONSE`, `INVALID_EVENT`, `UNKNOWN_TIMEZONE`, `EMPTY` se mantienen visibles en health; no se fabrican eventos ni se hace scraping.

Resultado offline: pruebas deterministas de normalización USD/EUR, caché, no-lookahead, fecha sin zona, ausencia de clave y alertas HIGH pasan. Resultado live: **BLOCKED / NOT_CERTIFIED**. La clave está presente localmente; la consulta mínima al Economic Calendar del 2026-09-17 23:23:43 UTC recibió HTTP 403 (`AUTH_ERROR`). No se recibieron eventos para evaluar cobertura USD/EUR ni utilidad para XAUUSD/EURUSD. Con el plan Free rechazado, Finnhub queda **SUPPORTED / NOT_ACTIVE** y no se reintenta. FRED/ALFRED también resultó insuficiente como fuente única; ver `FRED_ALFRED_EVALUATION.md`. El scheduler permanece desactivado.
